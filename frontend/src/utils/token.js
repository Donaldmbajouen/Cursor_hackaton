/**
 * token.js — Stockage / lecture JWT ou tokens session (localStorage)
 * Responsable : Dev Frontend (sécurité)
 */

// TODO Dev : clés namespaced, secure flags si cookies, refresh
export function saveToken(/* key, token */) {
  // TODO
}

export function readToken(/* key */) {
  // TODO
  return null;
}
