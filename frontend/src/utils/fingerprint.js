/**
 * fingerprint.js — Génération d’empreinte côté client
 * Responsable : Dev Frontend (sécurité)
 */

// TODO Dev : combiner canvas, UA, timezone, etc. — respecter le RGPD
export async function getFingerprint() {
  return 'TODO-fingerprint';
}
