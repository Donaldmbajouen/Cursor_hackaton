/**
 * Countdown.jsx — Compte à rebours jusqu’à closes_at
 * Responsable : Dev Frontend
 */

// TODO Dev : useEffect + interval, props closesAt (ISO), callback onEnd
export default function Countdown() {
  return <span>--:--</span>;
}
