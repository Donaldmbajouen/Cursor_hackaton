/**
 * jwtAuth.js — Vérification JWT pour routes protégées
 * Responsable : Dev Backend (sécurité)
 */
// TODO Dev : import jwt from 'jsonwebtoken';

// TODO Dev : extraire Bearer, valider la signature, attacher req.user / req.poll
export function jwtAuth(req, res, next) {
  next();
}
