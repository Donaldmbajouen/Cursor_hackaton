/**
 * seed.js — Données de démo / seed pour développement
 * Responsable : Dev Backend (DB)
 */
// TODO Dev : import db from './database.js';

// TODO : insérer sondages / votes de test si besoin
export function seed() {
  // TODO Dev : implémenter le seed idempotent
}
