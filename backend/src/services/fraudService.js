/**
 * fraudService.js — Scoring / décisions anti-fraude
 * Responsable : Dev 1 (Senior - Anti-fraude)
 */

// TODO Dev 1 : analyser fingerprint, ip_hash, historique sessions
export function evaluateRisk(/* context */) {
  return { allowed: true, score: 0 };
}
