/**
 * voteService.js — Enregistrement et agrégation des votes
 * Responsable : Dev Backend (domaine)
 */
// TODO Dev : import db from '../db/database.js';

// TODO Dev : castVote, getResultsForPoll, dédoublonnage…
export function castVote(/* pollId, optionIndex, ctx */) {
  return null;
}
