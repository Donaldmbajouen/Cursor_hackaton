/**
 * pollService.js — Logique métier sondages (CRUD SQLite)
 * Responsable : Dev Backend (domaine)
 */
// TODO Dev : import db from '../db/database.js';

// TODO Dev : createPoll, getPollById, listPolls, closePoll…
export function getPollById(pollId) {
  return null;
}
