/**
 * jwtService.js — Émission et vérification des JWT (sessions vote)
 * Responsable : Dev Backend (sécurité)
 */
// TODO Dev : import jwt from 'jsonwebtoken';

// TODO Dev : utiliser process.env.JWT_SECRET, choisir algorithmes et durées
export function signToken(/* payload */) {
  // TODO
  return null;
}

export function verifyToken(/* token */) {
  // TODO
  return null;
}
